# Profile

在 Cargo.toml中，可以对各个profle进行配置

```rust
[profile.dev]
opt-level = 0
```

```rust
[profile.release]
opt-level = 3
```

默认是 dev test bench release 这个四个profile，可以再额外增加一个eu profile
```rust
[profile.eu]
opt-level = 2
```
